# 0x1A. Application server

## Task 0: Set up development with Python

### Steps performed on web-01

1. Installed `net-tools`:
2. Cloned the AirBnB_clone_v2 repository.
3. Modified `web_flask/0-hello_route.py` to serve `Hello HBNB!` on route `/airbnb-onepage/` using Flask.
4. Ran the application with `python3 -m web_flask.0-hello_route` and verified it responds correctly on `http://127.0.0.1:5000/airbnb-onepage/`.

All requirements are met: Flask app object named `app`, port 5000, route `/airbnb-onepage/`.
